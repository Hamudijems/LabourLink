// This script creates the admin user in Firebase Auth
// You can run this in the browser console on your Firebase project

// Go to Firebase Console > Authentication > Users > Add User
// Email: hamudijems4@gmail.com
// Password: ahmed123

// Or run this in the browser console:
/*
import { getAuth, createUserWithEmailAndPassword } from 'firebase/auth';

const auth = getAuth();
createUserWithEmailAndPassword(auth, 'hamudijems4@gmail.com', 'ahmed123')
  .then((userCredential) => {
    console.log('Admin user created:', userCredential.user.uid);
  })
  .catch((error) => {
    console.error('Error creating admin user:', error);
  });
*/

console.log('To create admin user:');
console.log('1. Go to Firebase Console');
console.log('2. Navigate to Authentication > Users');
console.log('3. Click "Add User"');
console.log('4. Email: hamudijems4@gmail.com');
console.log('5. Password: ahmed123');